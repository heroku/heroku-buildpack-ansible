

install_ansible() 
{
	local buildDir=${1}
	
	cd /tmp
	git clone git://github.com/ansible/ansible.git

	ANSIBLE_INSTALL_DIR=/tmp/ansible	
	export PATH=$ANSIBLE_INSTALL_DIR/bin:$PATH
	
	echo "ansible has been installed at $(cd $(dirname $ANSIBLE_INSTALL_DIR); pwd)" 
}